/* Generated by TestGen on Wed Jun 7 2023 at 2:00 PM (GMT) */
@isTest
private class ChatbotController_TGN_TEST{
    private class ChatbotController_HCM_TGN_TEST implements HttpCalloutMock{
        protected Integer code;
        protected String status;
        protected String body;
        protected Map<String,String> headers;
        public ChatbotController_HCM_TGN_TEST(Integer code,String status,String body,Map<String,String> headers){
            this.code=code;
            this.status=status;
            this.body=body;
            this.headers=headers;
        }
        public HTTPResponse respond(HTTPRequest req){
            HttpResponse res=new HttpResponse();
            res.setStatusCode(this.code);
            res.setStatus(this.status);
            res.setBody(this.body);
            if(this.headers!=null){
                for(String key:this.headers.keySet()){
                    res.setHeader(key,this.headers.get(key));
                }
            }
            return res;
        }
    }
    @isTest
    private static void getObj_TGN_TEST(){
        Test.startTest();
        ChatbotController.getObj();
        Test.stopTest();
    }
    @isTest
    private static void CreateField_TGN_TEST(){
        Test.startTest();
        ChatbotController_HCM_TGN_TEST httpMock;
        httpMock=new ChatbotController_HCM_TGN_TEST(200,'Complete','{}',null);
        Test.setMock(HttpCalloutMock.class,httpMock);
        ChatbotController.CreateField('a','a','a','a');
        httpMock=new ChatbotController_HCM_TGN_TEST(200,'Complete','{}',null);
        Test.setMock(HttpCalloutMock.class,httpMock);
        ChatbotController.CreateField('','','','');
        Test.stopTest();
    }
    @isTest
    private static void ChatbotController_TGN_TEST(){
        ChatbotController obj = new ChatbotController();
    }
}